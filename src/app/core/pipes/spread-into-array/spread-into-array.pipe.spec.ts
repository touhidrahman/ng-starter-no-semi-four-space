import { SpreadIntoArrayPipe } from './spread-into-array.pipe'

describe('SpreadIntoArrayPipe', () => {
    it('create an instance', () => {
        const pipe = new SpreadIntoArrayPipe()
        expect(pipe).toBeTruthy()
    })
})
